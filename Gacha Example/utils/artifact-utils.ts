export const getArtifactStats = (artifact: any) => {
  const atk = 0
  const hp = 0
  const def = 0
  const crit_rate = 0
  const crit_dmg = 0

  // Implement logic to calculate artifact stats based on main and substates
  // This is a placeholder, replace with actual calculations

  return {
    atk,
    hp,
    def,
    crit_rate,
    crit_dmg,
  }
}
